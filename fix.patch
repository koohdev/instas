--- components/studio/batch-queue-panel.tsx
+++ components/studio/batch-queue-panel.tsx
@@ -48,29 +48,19 @@
     const urls = urlsInput
       .split("\n")
       .map((u) => u.trim())
-<<<<<<< Updated upstream
-      .filter(Boolean);
-    if (urls.length === 0) return;
-
-    // Validate URLs
-    const invalidUrls: string[] = [];
-    for (const url of urls) {
-      try {
-        new URL(url);
-      } catch (e) {
-        invalidUrls.push(url);
-      }
-    }
-
-    if (invalidUrls.length > 0) {
-      window.alert(`The following URLs are invalid:\n${invalidUrls.join("\n")}\n\nPlease correct them before adding to the queue.`);
-      return;
-    }
-
-=======
       .filter((u) => Boolean(u) && /^https?:\/\//i.test(u));
+
     if (urls.length === 0) {
       alert("No valid URLs found. Please ensure each URL starts with http:// or https://");
       return;
     }
->>>>>>> Stashed changes
+
+    // Validate URLs strictly
+    const invalidUrls: string[] = [];
+    for (const url of urls) {
+      try {
+        new URL(url);
+      } catch (e) {
+        invalidUrls.push(url);
+      }
+    }
+
+    if (invalidUrls.length > 0) {
+      window.alert(`The following URLs are invalid:\n${invalidUrls.join("\n")}\n\nPlease correct them before adding to the queue.`);
+      return;
+    }
+
